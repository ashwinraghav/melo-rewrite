terraform {
  required_version = ">= 1.5.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 5.0"
    }
  }

  # GCS backend — Terraform state stored in a Cloud Storage bucket.
  # The bucket must be created manually before running `terraform init`.
  # See docs/deploy.md for one-time bootstrap instructions.
  backend "gcs" {
    bucket = "melo-f5756-tfstate"
    prefix = "terraform/state"
  }
}

provider "google" {
  project = var.project_id
  region  = var.region
}

provider "google-beta" {
  project = var.project_id
  region  = var.region
}
